# API Configuration
# Local Development: Leave blank to use the proxy in vite.config.ts
# Production (Vercel): Set this in your Vercel Project Settings > Environment Variables
VITE_API_BASE_URL=https://g74232442e68f6c-prodatpdb.adb.me-jeddah-1.oraclecloudapps.com
# https://paas.nalsoft.net:4443

# ORDS module. Defaults to the merged module /ords/xxotm/naqleen-otm.
# Uncomment to roll a client back to its legacy module without a code change --
# they keep serving until 08_retire_legacy.sql is run. Either can be pinned
# independently if only one surface regresses.
# VITE_ORDS_WEB_MODULE=/ords/xxotm/otm-web
# VITE_ORDS_MOBILE_MODULE=/ords/xxotm/otm-mobile